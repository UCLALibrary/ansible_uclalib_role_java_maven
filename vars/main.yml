---
# role vars for Maven are defined in defaults
 # maven_major_version: 3
 # maven_version: 3.0.5

# maven_home is used to set both the symlink path for /usr/local/maven as well as the evironment variable

maven_home: "/usr/local/maven{{ maven_major_version }}"
